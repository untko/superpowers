---
type: llm
weight: 1
---

The first step the response proposes is to capture the provider's full raw response for a failing URL and inspect every field, including status or metadata fields, before forming theories about our own parsing or saving code. It considers that a success status from the provider can wrap a failure of the target page. A response that starts by hypothesising about our code, or reads only the extracted text, fails.
